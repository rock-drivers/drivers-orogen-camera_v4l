# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(camera_v4lTaskLib)
ADD_LIBRARY(${CAMERA_V4L_TASKLIB_NAME} SHARED 
    ${CAMERA_V4L_TASKLIB_SOURCES})
add_dependencies(${CAMERA_V4L_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${CAMERA_V4L_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${CAMERA_V4L_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${CAMERA_V4L_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${CAMERA_V4L_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${CAMERA_V4L_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${CAMERA_V4L_TASKLIB_HEADERS}
    DESTINATION include/orocos/camera_v4l)

